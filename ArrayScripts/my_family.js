let names = ["Kevin", "Kemely", "Kenny", "Kelly"];
let oldest = names[0];
let middle = names[1];
let secmiddle = names[2];
let youngest = names[3];

for (let i = 0; i < 4; i++) {
    console.log(names[i]);
}
